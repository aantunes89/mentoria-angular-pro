export type Product = {
  createdAt: string;
  name: string;
  price: string;
  description: string;
  image: string;
  id: string;
  quantity: number;
};

// Pq type? Na pratica é igual
// Faz mais sentido interfaces para metodos

// Para tipos é melhor usar type
